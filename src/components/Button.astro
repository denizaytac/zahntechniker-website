---
const { href, class: className, ...props } = Astro.props;
---

<a href={href} class={`inline-block px-6 py-3 rounded-md text-white bg-primary hover:bg-primary-dark transition-colors ${className}`} {...props}>
  <slot />
</a>
